// 変数の宣言(体重)
let bodyWeight;

// 値の代入(体重)
bodyWeight = 68;

// 変数の宣言 (身長)
let height;

// 値の代入(身長)
height = 1.7;

// BMIの計算
const bmi = bodyWeight / (height * height);

// コンソールへの出力
console.log(bmi);